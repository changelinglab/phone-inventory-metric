# Phone Inventory Metric

## Usage

```
from phone_inventory_metric import get_metrics

# Corpora are of type Iterable[str] (e.g., list[str])
# Return value is dict[str, float]
get_metrics(ref_corpus, target_corpus)
```
